MATLAB支持mavros2必须每一个MATLAB版本对应一个mavros2编译包。平台默认支持的mavros2的MATLAB版本是MATLAB2024a和MATLAB2024b，其它版本需自己编译mavros2。ros2是matlab从2019年才开始支持，需要几个版本迭代后才稳定，建议使用MATLAB2023a以上版本使用ros2，那样面临的问题会少一些。
执行命令ros2RegisterMessages(dir)即可将自定义消息注册到MATLAB，其中dir表示包含matlab_msg_gen.zip的目录。